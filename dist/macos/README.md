# BAGS_LAB — macOS portable runtime (legacy path)

Prefer the architecture-specific directories:

- `dist/macos-x64/` — Intel Mac (**NOT YET TESTED**)
- `dist/macos-arm64/` — Apple Silicon (**NOT YET TESTED**)

This folder remains only as a compatibility pointer. Do not place fabricated
binaries here.
